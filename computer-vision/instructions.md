### Download these files and cd into the folder, from there run these commands in a terminal in this order:

```python -m venv cv_env```

```cv_env\Scripts\activate```

```pip install -r requirements.txt```

#### Read each line of code and try your best to understand it, try to fix the bug.