## You will need to install the speech recognition software, in vs code at the bottom where it says terminal type this command into where the type cursor is (the white rectangle)

```pip install SpeechRecognition pyaudio```

if the above does NOT work let me know, and try installing these:

```pip install pipwin```


#### Once you are done installing look at listener.py for the code and pay attention to the comments!

#### You are still using easytello so dont forget to have the boilerplate for that at the top!!!